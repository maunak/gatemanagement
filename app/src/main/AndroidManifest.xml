<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gate">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/door"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gate">
        <activity android:name=".entrylist"></activity>
        <activity android:name=".workers" />
        <activity android:name=".fooddelivery" />
        <activity android:name=".courier" />
        <activity android:name=".visitors" />
        <activity android:name=".Dashboard" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>